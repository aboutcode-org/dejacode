name: dejacode

x-description: "Production overrides — build from source, external nginx config"

services:
  web:
    build: .

  worker:
    build: .

  scheduler:
    build: .

  nginx:
    configs: []
    volumes:
      - ./etc/nginx/conf.d/:/etc/nginx/conf.d/
